class ElectronicsService {
  String? image;
  String? title;
  String? price;
  String? rating;
  String? discount;

  ElectronicsService(
      {this.image, this.title, this.price, this.rating, this.discount});
}
