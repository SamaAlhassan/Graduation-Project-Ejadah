class ApplianceService {
  String? image;
  String? title;
  String? price;
  String? rating;
  String? discount;

  ApplianceService(
      {this.image, this.title, this.price, this.rating, this.discount});
}
