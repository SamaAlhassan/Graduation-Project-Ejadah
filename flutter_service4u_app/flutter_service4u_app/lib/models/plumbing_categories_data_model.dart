class PlumbingService {
  String? image;
  String? title;
  String? price;
  String? rating;
  String? discount;

  PlumbingService(
      {this.image, this.title, this.price, this.rating, this.discount});
}
