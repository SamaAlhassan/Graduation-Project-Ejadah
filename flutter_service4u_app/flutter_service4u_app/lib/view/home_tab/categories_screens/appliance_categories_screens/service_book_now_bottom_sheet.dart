import 'package:flutter/material.dart';

class ServiceBookBottomSheet extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      padding: EdgeInsets.all(20),
      child: Text('Service Booking Details'),
    );
  }
}
